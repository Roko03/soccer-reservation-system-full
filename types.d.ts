

type Reservation = {
    name: string;
    phoneNumber: number;
    startDateTime: string;
}

type User = {
    username: string;
    email: string;
    password: string;
}

type LoginData = {
    email: string;
    password: string;
}
